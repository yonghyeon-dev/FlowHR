owner: people-agent
version: 0.3.21
scope:
  in:
    - organization master data (create/read/list)
    - employee master data (create/read/list/update)
    - department master data (create/read/list/update)
    - position master data (create/read/list/update/delete) with title/grade/description
    - employee profile assignment with department/position consistency checks
    - audit logs for all employee/org mutations
    - domain events for employee/org mutations
    - notice/recruitment/benefits workspace persistence read-model tables for organization-scoped employee communication and ATS/self-service journey
    - benefits catalog status lifecycle controls and inactive-catalog request guard
    - benefits catalog deactivate guard for pending submitted requests
    - benefits request queue priority sorting (`sort=pending_priority`) for submitted-first review ordering
    - recruitment referral queue priority sorting (`sort=stalled_priority`) for active stalled-referral-first review ordering
    - admin organization settings read/update API for post-onboarding organization configuration changes
    - admin-managed notification default settings with durable employee preference overrides
    - admin-managed operator alert webhook fallback settings for approval escalation and leave-promotion flows
    - employee lifecycle status model (`ACTIVE`/`ON_LEAVE`/`RESIGNED`) with controlled transitions and resigned-session invalidation
    - onboarding default task template persistence and ACTIVE-state auto-assignment
  out:
    - multi-tenant enforcement (RLS) across core tables (Phase 1 follow-up)
    - RBAC engine and permission administration UI (Phase 1 follow-up)
    - attendance/leave/payroll business rules
entities:
  - name: Organization
    description: Company/tenant placeholder entity used for future tenant isolation.
  - name: Department
    description: Organization-level department catalog with unique code per organization.
  - name: Position
    description: Organization-level position catalog with unique code per organization.
  - name: Employee
    description: HR master entity. Canonical id should match Supabase auth user.id (UUID string) for production.
  - name: RecruitmentOpening
    description: Organization-scoped recruitment opening read model for ATS core journey.
  - name: RecruitmentReferral
    description: Organization-scoped referral pipeline read model linked to openings.
  - name: BenefitCatalogItem
    description: Organization-scoped benefits catalog read model.
  - name: BenefitRequest
    description: Organization-scoped benefits request read model linked to benefit catalog items.
  - name: InAppNotification
    description: Organization-scoped in-app notification center read model for employee/admin self-service alerts.
  - name: Notice
    description: Organization-scoped announcement read model supporting draft/scheduled partial updates before publish lock.
  - name: OnboardingTask
    description: Employee onboarding checklist task projection copied from onboarding task templates.
  - name: OnboardingTaskTemplate
    description: Default onboarding checklist template catalog used to seed employee onboarding tasks.
api:
  auth:
    - role: admin
      permission: manage employees and organizations
  endpoints:
    - method: GET
      path: /people/organizations
      description: List organizations.
    - method: POST
      path: /people/organizations
      description: Create organization.
    - method: GET
      path: /people/organizations/{organizationId}
      description: Get organization by id.
    - method: GET
      path: /admin/organization/settings
      description: Get current admin organization settings (name/businessNumber/fiscalYearStart/workHoursPerDay/overtimeThreshold/timezone).
    - method: PATCH
      path: /admin/organization/settings
      description: Update current admin organization settings for profile and work-hour policy fields.
    - method: GET
      path: /admin/notification-defaults
      description: Read organization-level default notification channels and categories.
    - method: PUT
      path: /admin/notification-defaults
      description: Update organization-level default notification channels and categories.
    - method: GET
      path: /admin/operator-alerts
      description: Read organization-level fallback webhook settings for operator alert flows.
    - method: PUT
      path: /admin/operator-alerts
      description: Update organization-level fallback webhook settings and enabled operator flows.
    - method: GET
      path: /people/employees
      description: "List employees (optional filters: `active`, `status`, `organizationId`)."
    - method: GET
      path: /employees
      description: "Alias endpoint to list employees (optional filters: `active`, `status`, `organizationId`)."
    - method: GET
      path: /people/departments
      description: List departments (optional active/organization filters).
    - method: POST
      path: /people/departments
      description: Create department with unique code per organization.
    - method: GET
      path: /people/departments/{departmentId}
      description: Get department by id.
    - method: PATCH
      path: /people/departments/{departmentId}
      description: Update department metadata.
    - method: DELETE
      path: /people/departments/{departmentId}
      description: Delete department (blocked when employees are assigned).
    - method: GET
      path: /people/positions
      description: List positions (optional active/organization filters).
    - method: POST
      path: /people/positions
      description: Create position with title/grade/description support and unique code per organization.
    - method: GET
      path: /people/positions/{positionId}
      description: Get position by id.
    - method: PATCH
      path: /people/positions/{positionId}
      description: Update position metadata (title/grade/description/code/name/active).
    - method: DELETE
      path: /people/positions/{positionId}
      description: Delete position (blocked when employees are assigned).
    - method: POST
      path: /people/employees
      description: Create employee (id must be provided; recommended Supabase user.id) with optional departmentId/positionId assignment.
    - method: GET
      path: /people/employees/{employeeId}
      description: Get employee by id.
    - method: GET
      path: /people/employees/{employeeId}/history
      description: List employee profile history events (created/profile-updated).
    - method: PATCH
      path: /people/employees/{employeeId}
      description: Update employee profile fields (admin only).
    - method: PATCH
      path: /employees/{id}/status
      description: Transition employee status with enforced transition rules (admin only).
    - method: GET
      path: /benefits/catalog
      description: List organization-scoped benefits catalog items.
    - method: PATCH
      path: /notices/{noticeId}
      description: Partially update draft/scheduled notices; at least one field is required and omitted fields are preserved.
    - method: POST
      path: /notices
      description: Create draft notices without `publishAt` and scheduled notices with an offset-aware `publishAt`.
    - method: POST
      path: /benefits/catalog
      description: Create organization-scoped benefits catalog item.
    - method: POST
      path: /benefits/catalog/{benefitId}/status
      description: Update benefits catalog item ACTIVE/INACTIVE status (admin/manager).
    - method: GET
      path: /benefits/requests
      description: List organization/employee-scoped benefits requests (optional `sort=pending_priority` for submitted-first queue ordering).
    - method: GET
      path: /recruitment/referrals
      description: List organization-scoped recruitment referrals (optional `sort=stalled_priority` for active stalled-referral-first queue ordering).
    - method: POST
      path: /benefits/requests
      description: Submit benefits request (inactive catalog items are rejected).
    - method: POST
      path: /benefits/requests/{requestId}/decision
      description: Approve/reject SUBMITTED benefits request (admin/manager only).
    - method: POST
      path: /benefits/requests/{requestId}/cancel
      description: Cancel own SUBMITTED benefits request (employee only).
    - method: GET
      path: /notifications
      description: List current actor in-app notifications (latest 50, optional unread filter).
    - method: PATCH
      path: /notifications/{notificationId}/read
      description: Mark own in-app notification as read.
    - method: POST
      path: /notifications/mark-all-read
      description: Mark all own in-app notifications as read.
    - method: GET
      path: /employee/notification-preferences
      description: Read employee notification preferences resolved against organization defaults.
    - method: PUT
      path: /employee/notification-preferences
      description: Save or reset employee notification preferences against organization defaults.
  events:
    published:
      - name: organization.created.v1
        description: Emitted when an organization record is created.
      - name: department.created.v1
        description: Emitted when a department record is created.
      - name: department.updated.v1
        description: Emitted when department metadata is updated.
      - name: position.created.v1
        description: Emitted when a position record is created.
      - name: position.updated.v1
        description: Emitted when position metadata is updated.
      - name: employee.created.v1
        description: Emitted when an employee record is created.
      - name: employee.profile.updated.v1
        description: Emitted when employee profile metadata is updated (name/email/org/active).
      - name: employee.status.transitioned.v1
        description: Emitted when employee status transition succeeds.
    consumed: []
db_changes:
  migrations:
    - id: 202602140005_employee_master
      description: Create Organization and Employee base tables.
    - id: 202602180003_people_department_position
      description: Add Department/Position tables and employee departmentId/positionId references.
    - id: 202603020001_wi0755_notices_db_read_model
      description: Add Notice, NoticeReadReceipt, and NoticeNotificationQueue models for notices persistence/read-model.
    - id: 202603020002_wi0757_recruitment_db_persistence
      description: Add RecruitmentOpening and RecruitmentReferral models for ATS opening/referral persistence.
    - id: 202603030001_wi0816_benefits_db_read_model
      description: Add BenefitCatalogItem and BenefitRequest models for benefits persistence/read-model.
    - id: 202603050001_wi0908_notice_department_targeting
      description: Add optional Notice targetDepartmentIds column for department-targeted notice delivery.
    - id: 202603050002_sync_wi0910_to_wi0917
      description: Sync onboarding/insurance models and employee/recruitment/benefits profile fields with Prisma persistence schema.
    - id: 202603050003_wi0922_onboarding_wizard
      description: Add organization onboarding setup fields and onboarding completion flag for first-admin login wizard.
    - id: 202603050005_wi0926_notification_center
      description: Add InAppNotification model for in-app notification center read and bulk-read APIs.
    - id: 202603050006_wi0932_org_settings
      description: Add organization fiscal year start and work-hour policy settings for admin organization settings API.
    - id: 202603050008_wi0938_department_management
      description: Add Department parent/manager references and admin CRUD management support.
    - id: 202603050009_wi0940_position_management
      description: Add Position title/grade/description fields and admin delete guard support.
    - id: 202603050011_add_employee_status
      description: Replace Employee.active with Employee.status enum and add employee status transition support.
    - id: 202603050012_wi0955_org_settings_api
      description: Add organization fiscal year, work hours, overtime, pay period, and currency settings columns.
    - id: 202603060001_wi0998_onboarding_default_tasks_seed
      description: Rename onboarding task projection table and add onboarding task template persistence for default seeding.
    - id: 202603090003_wi1055_notification_durability_productization
      description: Add organization notification defaults and durable employee notification preference fields.
    - id: 202603090004_wi1055_operator_alert_webhook_productization
      description: Add organization-level operator alert webhook fallback settings for approval escalation and leave-promotion flows.
  backward_compatible: true
  notes: "Additive foundation tables only. Department/Position are organization-scoped catalogs, recruitment/benefits read models remain additive, and onboarding template persistence is added for default task seeding."
invariants:
  - Employee mutations are admin-only in v0.1.
  - Employee id is treated as canonical identifier and should match Supabase auth user.id in production.
  - Organization is a placeholder for future tenant isolation (no RLS enforced by this contract).
  - Department code must be unique within an organization.
  - Position code must be unique within an organization.
  - Position delete is blocked when employees are assigned to the position.
  - Employee department and position assignments must match employee organization scope.
  - Employee notification preferences inherit organization defaults when employee-specific values are null.
  - Approval escalation and leave-promotion webhook dispatch read organization fallback webhook settings before env fallback when the respective flow toggle is enabled.
  - Employee status transitions are restricted to ACTIVE<->ON_LEAVE and ACTIVE/ON_LEAVE->RESIGNED.
  - RESIGNED is terminal and cannot transition to another status.
risk:
  permissions:
    - Unauthorized employee creation could lead to data integrity issues once FK enforcement is introduced.
    - Unauthorized department/position updates may impact workforce reporting and approval routing.
  legal:
    - Employee PII must be minimized and audited.
test_plan:
  unit:
    - create/update payload validation boundaries (email, name length)
    - department/position payload and query validation boundaries
    - list query filter parsing
  integration:
    - organization create/list/get admin-only
    - employee create/list/get/update admin-only
    - employee status transition API validates allowed transitions and terminal resigned state
    - employee list supports status filter (`ACTIVE`/`ON_LEAVE`/`RESIGNED`)
    - employee history list (created/profile-updated) returns most recent entries first
    - department/position create/list/get/update admin-only
    - employee org mismatch with department/position returns 409
    - duplicate employee create returns 409
    - duplicate department/position code in same organization returns 409
    - benefits catalog status update endpoint allows ACTIVE/INACTIVE lifecycle updates for admin/manager
    - benefits request create returns 409 when target catalog item is INACTIVE
    - benefits request decision endpoint rejects non-SUBMITTED requests with invalid_state
    - benefits catalog status endpoint rejects INACTIVE transition while submitted requests remain
    - benefits request list supports `sort=pending_priority` for submitted-first queue ordering
    - recruitment referral list supports `sort=stalled_priority` for stalled active-referral-first queue ordering
    - notice patch supports partial draft/scheduled updates and rejects empty payloads with 400
    - notice create accepts omitted or null `publishAt` for draft creation and offset-aware `publishAt` for scheduled creation
    - admin notification-defaults read/update persists organization-scoped defaults
    - admin operator-alerts read/update persists organization-scoped fallback webhook settings
    - employee notification-preferences read resolves employee values against organization defaults
    - employee notification-preferences reset clears employee overrides back to organization defaults
  regression:
    - existing attendance/leave/payroll flows remain unchanged
  authorization:
    - non-admin calls are rejected with 403
  payroll_accuracy:
    - not applicable
observability:
  audit_events:
    - organization.created
    - department.created
    - department.updated
    - position.created
    - position.updated
    - position.deleted
    - employee.created
    - employee.profile.updated
    - employee.status.transitioned
  metrics:
    - employee_created_count
    - employee_updated_count
    - employee_status_transition_count
    - department_created_count
    - position_created_count
    - position_deleted_count
  logs:
    - authorization denied logs for people endpoints
rollout:
  feature_flags:
    - name: people_v1
      default: "off"
  migration_strategy: expand-contract
rollback:
  strategy: disable people_v1 and revert API exposure; data remains for forward-fix
  max_recovery_time: 30m
deprecations: []
breaking_changes: false
consumer_impact: "People contract v0.3.21 additively documents notice creation draft/scheduled semantics while preserving backward compatibility for existing people APIs."
references:
  - work-items/WI-0034-employee-organization-master-model.md
  - work-items/WI-0102-people-department-position-model.md
  - work-items/WI-0755-notices-db-persistence-and-notification-read-model.md
  - work-items/WI-0757-recruitment-db-persistence-core-journey.md
  - work-items/WI-0816-benefits-db-persistence-read-model.md
  - work-items/WI-0817-benefits-catalog-status-lifecycle-and-inactive-guard.md
  - work-items/WI-0819-benefits-catalog-deactivate-pending-guard.md
  - work-items/WI-0820-benefits-request-pending-priority-sort.md
  - work-items/WI-0821-recruitment-referral-stalled-priority-sort.md
  - work-items/WI-0908-notice-department-targeting.md
  - work-items/WI-0920-schema-sync.md
  - work-items/WI-0926-notification-center.md
  - work-items/WI-0932-org-settings.md
  - work-items/WI-0938-department-management.md
  - work-items/WI-0940-position-management.md
  - work-items/WI-0952-employee-status-transitions.md
  - work-items/WI-0998-onboarding-default-tasks-seed.md
  - work-items/WI-1030-announcement-partial-update.md
  - work-items/WI-1042-notice-create-publishAt-nullable.md
  - work-items/WI-1050-notice-create-publishAt-fix.md
  - work-items/WI-1055-admin-operational-settings-productization.md
  - docs/data-ownership.md
approval:
  spec_owner: people-agent
  qa_owner: qa-agent

