-- CreateSchema CREATE SCHEMA IF NOT EXISTS "public";  -- CreateEnum CREATE TYPE "AttendanceState" AS ENUM ('PENDING', 'APPROVED', 'REJECTED');  -- CreateEnum CREATE TYPE "PayrollState" AS ENUM ('PREVIEWED', 'CONFIRMED');  -- CreateTable CREATE TABLE "AttendanceRecord" (     "id" TEXT NOT NULL,     "employeeId" TEXT NOT NULL,     "checkInAt" TIMESTAMP(3) NOT NULL,     "checkOutAt" TIMESTAMP(3),     "breakMinutes" INTEGER NOT NULL DEFAULT 0,     "state" "AttendanceState" NOT NULL DEFAULT 'PENDING',     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,     "updatedAt" TIMESTAMP(3) NOT NULL,      CONSTRAINT "AttendanceRecord_pkey" PRIMARY KEY ("id") );  -- CreateTable CREATE TABLE "PayrollRun" (     "id" TEXT NOT NULL,     "periodStart" TIMESTAMP(3) NOT NULL,     "periodEnd" TIMESTAMP(3) NOT NULL,     "state" "PayrollState" NOT NULL DEFAULT 'PREVIEWED',     "grossPayKrw" INTEGER NOT NULL,     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,     "updatedAt" TIMESTAMP(3) NOT NULL,      CONSTRAINT "PayrollRun_pkey" PRIMARY KEY ("id") );  
